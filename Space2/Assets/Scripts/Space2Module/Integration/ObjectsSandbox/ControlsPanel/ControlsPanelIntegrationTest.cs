using UnityEngine;

namespace Assets.Scripts.Space2Module.Integration.ObjectsSandbox.ControlsPanel
{
    public class ControlsPanelIntegrationTest : MonoBehaviour
    {
        void Start ()
        {
            /*
                TODO
                    Ascertain values shown are player values
                        have player
                        get values from view
                        update and compare a few times
                    Ascertain changes from view are reflected correctly
                        have player
                        initialize appropriate button actions in view
                        go over all changes, compare              
                        make sure view displays upcoming changes to values in addition to values
                    Ascertain panel is shown when speed is 0 and hidden when speed != 0
                    Ascertain changes to value are correctly applied
            */
        }
    }
}
